spring:
  main:
    allow-circular-references: true

  data:
    mongodb:
      port: 0
      database: test

  shell:
    interactive:
      enabled: false

  mongodb:
    embedded:
      version: 3.2.8


logging:
  level:
    root: ERROR
